/* lab.min.js */(()=>{'use strict';// Assemble three tokens using underscores.
// Sources: image tail, DNS TXT, odd path in logs.
// Normalize each token to UPPERCASE before submission.
// Example: UPPER(tkn_img) + '_' + UPPER(tkn_dns) + '_' + UPPER(tkn_log)
console.log('glass-ok');})();
